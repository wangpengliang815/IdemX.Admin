---
description: "后端 Application 单元测试：命名、Mock、断言与结构约定。"
globs: tests/backend/**/*.cs
alwaysApply: false
---

# 单元测试规范

> 动 `tests/` 须用户明确要求（见工作协议）。本规则在编写/修改后端单元测试时生效。

## 锚文件

`tests/backend/unit/Core.Application.UnitTests/System/SysOrgServiceTests.cs`

## 命名

| 规则 | 说明 |
|------|------|
| 方法名简短 | `{被测方法}_{场景}`，如 `GetById_NotFound`、`Delete_Subtree` |
| 禁止冗长 | 不用 `When..._Returns...`、不写 `Async` 后缀（方法名已表达） |
| 测试数据 | 固定树等小工厂用短名，如 `Tree123()` |

## 字段与构造

| 规则 | 说明 |
|------|------|
| 禁止 `_sut` | 被测服务命名为 `service`（或 `xxxService`），不用 `_sut` |
| 禁止 `_repo` 前缀 | Mock 字段用 `orgRepo`、`userRepo`，**不用**前导下划线 |
| 映射 | `MapperFixture.Create()`，与线上一致 |

## 结构与注释

| 规则 | 说明 |
|------|------|
| 不写套话 | 不要类级「参考用例」长文档；不要机械 `// Arrange` / `// Act` / `// Assert` |
| 共用 Mock | 重复的 `GetListAsync(条件)` 过滤可抽 `SetupOrgListFilter(all)` 等短名辅助方法 |

## 断言

| 规则 | 说明 |
|------|------|
| 成功/失败 | `Code == 0` 成功，`Code == 1` 失败 |
| 消息 | 用 `GlobalConstVars.Xxx`，禁止硬编码中文 |
| 数据 | 关键 Id、父子关系、标志位（如 `HasChild`）要断言；随机字段用 Bogus，不断言无意义字符串 |
| Verify | 删除、批量写库等须 `Verify` 调用参数（如 `IEnumerable<long>`，与仓储签名一致） |

## Mock 注意

| 规则 | 说明 |
|------|------|
| `GetListAsync` 三参 | Service 带条件/排序时，Setup 用 `It.IsAny<Expression<...>>()` + `It.IsAny<OrderByType>()`；`orderBy` 为 `null` 时 `It.IsAny` 须能匹配 |
| 无参 `GetListAsync()` | 等价 `GetListAsync(null, null, OrderByType.Desc)`，Setup 须与 IL 一致 |
| 集合类型 | 仓储若收 `IEnumerable<long>`，Verify/Setup 勿写死 `List<long>` |

## 数据

- `Bogus` 生成 `Name`、`Sort` 等；**Id、ParentId、业务关系在用例里显式赋值**
- 集合初始化可用 `[..]`、`[item]`，与后端规范一致

## 不要测

- 纯 DTO 映射、无分支的 getter
- 为「覆盖率」重复测同一失败路径
