---
description: "C# 后端编码规范：Controller/Service/DTO/Entity 模板与硬约束。"
globs: src/backend/**/*.cs
alwaysApply: false
---

# 后端规范

## 硬约束

| 规则 | 说明 |
|------|------|
| Controller 不写 try-catch | 全局异常处理兜底 |
| Controller 不注入 Repo / SqlSugar | 只能注入 IService |
| DTO 后缀用 `Req` / `Resp` / `PageQueryReq` | 禁止 Request / Response |
| 编辑/删除 id 走路径参数 | `Edit(long id, [FromBody] Req)` / `Del(long id)` |
| 成功路径不做防御兜底 | 禁止 `?? []` / `?? 0` / `?? '-'` |
| 空值判断优先 `is null` / `is not null` | 新代码优先使用；存量 `== null` / `!= null` 不强制改 |
| 禁止 null 宽恕运算符 | 严格禁止 `!`（如 `x!`、`p.ParentId!.Value`）；用 `is not null` 收窄后再访问，或 `Where` + 访问 `.Value` |
| 文件作用域 namespace | 禁止块级 namespace |
| 改 Entity 只改 C# | 不生成/改 DB 脚本 |
| 注释句末不加句号 | `///`、`//` 等注释正文**句末不加句号**（含中文 `。`、英文 `.`）；Agent 生成注释时遵守 |
| 接口不写注释 | `Interfaces/` 下 `I*Service` **不写**任何 `///` 文档注释（含类、方法） |
| 实现须写注释 | `Services/` 等实现类：**类 + 每个 public 方法**须有简洁中文 `/// <summary>`（一行概括，句末不加句号）；**禁止** `inheritdoc` |
| 集合用简化写法 | 分析器可简化（如 IDE0305）时用 C# 12 集合表达式：`return [.. items];`、`HashSet<T> x = [.. query];`，`[` 与 `..` **同一行**；勿在末尾多余 `.ToList()`，能传 `IEnumerable` 的不先转 `List` |

## Controller 模板

锚文件：`Controllers/System/SysUserController.cs`、`Controllers/System/SysOrgController.cs`

```csharp
namespace IdemX.Admin.Api.Controllers;

[Description("模块描述")]
public class XxxController(IXxxService xxxService) : AuthorizedControllerBase
{
    [HttpPost]
    [Description("获取列表")]
    public Task<CustomApiResponse<List<XxxResp>>> GetPageList([FromBody] XxxPageQueryReq request) =>
        xxxService.GetPageListAsync(request);

    [HttpGet("{id}")]
    [Description("根据Id获取单行数据")]
    public Task<CustomApiResponse<XxxResp>> GetById(long id) =>
        xxxService.GetByIdAsync(id);

    [HttpPost]
    [Description("创建")]
    public Task<CustomApiResponse> Create([FromBody] XxxReq request) =>
        xxxService.CreateAsync(request);

    [HttpPost("{id}")]
    [Description("编辑")]
    public Task<CustomApiResponse> Edit(long id, [FromBody] XxxReq request) =>
        xxxService.EditAsync(id, request);

    [HttpPost("{id}")]
    [Description("删除")]
    public Task<CustomApiResponse> Del(long id) =>
        xxxService.DeleteAsync(id);
}
```

## Service 模板

锚文件：`Core.Application/System/Services/SysUserService.cs`

```csharp
namespace Core.Application;

public class XxxService(IBaseRepo<Xxx> xxxRepo
    , IMapper mapper
    , IUnitOfWork unitOfWork
    , IHttpContextUser contextUser) : IXxxService
{
    public async Task<CustomApiResponse<List<XxxResp>>> GetPageListAsync(XxxPageQueryReq request)
    {
        var where = PredicateBuilder.True<Xxx>();

        if (!string.IsNullOrEmpty(request.Name))
            where = where.And(p => p.Name.Contains(request.Name));

        var list = await xxxRepo.GetPageAsync(where, p => p.Id, OrderByType.Desc, request.Page, request.PageSize);
        var dtoList = mapper.Map<List<XxxResp>>(list);
        return CustomApiResponse<List<XxxResp>>.Ok(GlobalConstVars.GetDataSuccess, dtoList, list.TotalCount);
    }
}
```

关键模式：
- 条件拼装用 `PredicateBuilder.True<T>()` + `.And(...)`
- 事务：`unitOfWork.BeginTranAsync()` → try → `CommitTranAsync()` → catch → `RollbackTranAsync()` + throw
- 成功/失败消息用 `GlobalConstVars.XxxSuccess` / `GlobalConstVars.XxxFailure`
- 数据不存在返回 `CustomApiResponse.Fail(GlobalConstVars.DataIsNo)`

### Service 内 private 方法

| 规则 | 说明 |
|------|------|
| ≥2 处调用才抽 | 同一 `private` 方法在本类内被调用 **≥2 次** 时才抽取；否则写在调用处 **内联** |
| 禁止 private 套 private | 不允许「`private` 仅被另一个 `private` 调用」；合并进外层 `private` 或内联到 `public` 方法 |
| 单处但很长 | 仅 **1 处** 调用、但连续约 **≥8 行** 且意图不明显时，可抽一个命名 `private`，名称须表达业务含义 |
| 对标锚文件 | 只抄业务步骤与数据访问方式，不照搬锚文件里多余的 `private` 拆分 |
| 显式 `private` | 必须写出 `private` 修饰符，**禁止**省略（不依赖默认访问级别） |
| 须有文档注释 | 每个 `private` 方法须写 `/// <summary>`（硬性），与 `public` 一致；**一行概括**即可，勿冗长，非必要不写 `<param>` / `<returns>`，句末不加句号 |
| 位于类最下方 | 所有 `private` 方法集中在类的 **最底部**，排在全部 `public` 方法之后 |

## DTO 模板

```csharp
// 分页查询
public class XxxPageQueryReq : BasePageQueryReq { ... }

// 创建/编辑请求
public class XxxReq { ... }

// 响应
public class XxxResp : BaseResp { ... }
```

## Entity 模板

锚文件：`Core.Model/System/Org/SysOrg.cs`

```csharp
namespace Core.Model.System;

[SugarTable("public.sys_org")]
public class SysOrg : BaseEntity
{
    [SugarColumn(IsNullable = true)]
    public long? ParentId { get; set; }

    [Required]
    [MaxLength(200)]
    public string Name { get; set; }

    [Required]
    public int Sort { get; set; }
}
```
