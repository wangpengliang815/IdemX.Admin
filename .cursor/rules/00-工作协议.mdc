---
alwaysApply: true
description: "协作基线：怎么干活、禁入目录、过度设计红线。"
---

# 工作协议

## 怎么干活

1. **小步走**：每次最多改 2 个文件。改完告诉我改了啥，等我确认再继续。
2. **先读后写**：动手改之前，先读 2 个同模块已有文件，对齐写法。
3. **不写套话**：回复不需要"任务理解/层级识别/规则拉取"这类模板开头，直接说事。
4. **不知道就问**：遇到不确定的符号、字段、逻辑，先去仓库检索真实代码。检索不到直接问我，不要猜。

## 禁入目录

- `src/frontend/jyd.jcpt.applet/`（小程序）
- `tests/`（我要求时才动）

## 过度设计红线

这不是大厂项目，以下情况**不要提**：

- ❌ 建议抽接口 — 除非当前确实有多个实现或多态需求
- ❌ 建议引入 MediatR / CQRS / 事件总线
- ❌ 建议拆分微服务或独立部署
- ❌ 建议做读写分离 / 分库分表
- ❌ 建议引入新的第三方库 — 除非当前写法有严重 Bug 且没有其他解法
- ❌ 建议"提取公共类/基类供未来复用" — 未来的需求不要现在设计
- ❌ 建议加缓存 — 除非你明确感知到性能问题

**什么值得提：**
- ✅ 当前代码**明确有 Bug**（空指针、数据不一致、异步死锁）
- ✅ 当前写法**一定会在数据量上来后出问题**（N+1、全表查询无分页）
- ✅ 当前代码**维护时会踩坑**（逻辑纠缠、方法太长、命名误导）
- ✅ 安全风险（没鉴权、SQL 注入、敏感数据暴露）
